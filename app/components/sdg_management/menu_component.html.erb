<ul class="sdg-content-menu">
  <li class="<%= "is-active" if sdg? %>">
    <%= link_to t("sdg_management.menu.sdg_content"), sdg_management_goals_path, class: "goals-link" %>
  </li>

  <% SDG::Related::RELATABLE_TYPES.each do |type| %>
    <%= relatable_list_item(type) %>
  <% end %>
</ul>
